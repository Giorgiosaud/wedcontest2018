@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card card-default">
                    <div class="card-header">Contests</div>

                    <div class="card-body">
                        @if (session('status'))
                            <div class="alert alert-success">
                                {{ session('status') }}
                            </div>
                        @endif
                        @foreach($contests as $contest)
                            <article>
                                <a href="{{$contest->path()}}"><h3>{{$contest->topic}}</h3></a>
                                <h4>{{$contest->year}}</h4>
                                <p>{{$contest->description}}</p>
                            </article>
                            <hr>
                        @endforeach
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
